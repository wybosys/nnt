use like:
    #pragma GCC diagnostic ignored "-Wconversion"
to ignore the warning (conversion)

map: 
     parentheses => Equality comparison with extraneous parentheses

use like:
    gcc -dM -E - < /dev/null
    clang -dM -E -x c /dev/null
to print all macros.

git config --global core.excludesfile ignorefile
